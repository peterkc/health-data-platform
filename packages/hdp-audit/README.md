# hdp-audit

Audit batches + changes + trigger helpers.

**Depth**: DEEP  (DEEP = functional + tests; MIN = skeleton + 1-2 things working; SKEL = interface only; COMPOSED = wires others)

TODO: Add audit_batches + audit_changes SQLModel tables and a trigger generator.
