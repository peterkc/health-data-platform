# hdp-ingest

Source adapter registry + base classes.

**Depth**: SKEL  (DEEP = functional + tests; MIN = skeleton + 1-2 things working; SKEL = interface only; COMPOSED = wires others)

TODO: Define the SourceAdapter base class + registry entry-point.
