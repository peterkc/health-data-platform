# hdp-agent

AI-agent-as-grantee interface + tool-use scaffolding.

**Depth**: SKEL  (DEEP = functional + tests; MIN = skeleton + 1-2 things working; SKEL = interface only; COMPOSED = wires others)

TODO: Define the Grantee/AgentTool interface for AI-agent-as-grantee flows.
