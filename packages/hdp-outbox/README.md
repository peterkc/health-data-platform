# hdp-outbox

Transactional outbox table + consumer interface.

**Depth**: SKEL  (DEEP = functional + tests; MIN = skeleton + 1-2 things working; SKEL = interface only; COMPOSED = wires others)

TODO: Add outbox SQLModel table + a pluggable Consumer interface.
