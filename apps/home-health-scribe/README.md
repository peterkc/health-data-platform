# home-health-scribe

FastAPI service composing hh-* + hdp-* for a runnable demo.

**Depth**: COMPOSED  (DEEP = functional + tests; MIN = skeleton + 1-2 things working; SKEL = interface only; COMPOSED = wires others)

TODO: Compose hh-scribe + hh-oasis + hh-hitl + hh-emr-sync into a single FastAPI app.
