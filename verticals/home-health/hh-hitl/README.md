# hh-hitl

Human-in-the-loop checkpoint protocol + API.

**Depth**: MIN  (DEEP = functional + tests; MIN = skeleton + 1-2 things working; SKEL = interface only; COMPOSED = wires others)

TODO: Implement the /checkpoints/{id}/approve FastAPI route backed by hdp-audit.
