# hh-mcp

MCP server exposing home-health scribe + HITL + sync as tools.

**Depth**: MIN  (DEEP = functional + tests; MIN = skeleton + 1-2 things working; SKEL = interface only; COMPOSED = wires others)

TODO: Expose scribe + HITL + sync as MCP tools behind hdp-api.
