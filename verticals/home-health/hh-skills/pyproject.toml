[project]
name = "hh-skills"
version = "0.1.0"
description = "Claude Code skills wrapping hh-mcp (SKILL.md + connectors)."
readme = "README.md"
requires-python = ">=3.13"
license = "MIT"
authors = [{ name = "Peter K. Chan", email = "peterkc@hey.com" }]
dependencies = [

]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hh_skills"]
